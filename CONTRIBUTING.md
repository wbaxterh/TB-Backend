# Contributing

Fork the repository, create a focused feature branch, and open a pull request against `master`. Run `npm ci` and `npm run validate` before submitting. Pull requests must pass automated checks, receive code-owner approval, and resolve all review threads. Maintainers squash-merge approved changes.

Never commit credentials or production data. New behavior should include tests where practical.
